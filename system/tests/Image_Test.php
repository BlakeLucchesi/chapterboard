<?php defined('SYSPATH') or die('No direct script access.');

class Image_Test extends Unit_Test_Case {
  
  /**
   * Test to make sure that we are clearing all images from the resize directories.
   */
  // public function clear_cache_test() {
  //   image::clear_cache();
  //   foreach (Kohana::config('image.sizes') as $dir => $value) {
  //     $path = Kohana::config('core.filepath').DIRECTORY_SEPARATOR.$dir;
  //     $this->assert_empty(glob($path.DIRECTORY_SEPARATOR.'*'));
  //   }
  // }
  
}